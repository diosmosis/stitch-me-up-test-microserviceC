namespace: microserviceC

public:
  - web

services:
  web:
    image: node:4
    ports:
      - "3000:3000"
    volumes:
      - ..:/microserviceC
    working_dir: /microserviceC
    command: [npm, run, start-mocks]

setup:
  - cd .. && npm install
