namespace: microserviceC

public:
  - web

services:
  web:
    image: node:4
    volumes:
      - .:/microserviceC
    working_dir: /microserviceC
    environment:
      - "MICROSERVICED_ENDPOINT=http://microserviceD"
    command: [npm, start]

dependencies:
  - microserviceD

setup:
  - npm install
